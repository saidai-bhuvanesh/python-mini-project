# Weather CLI

A simple CLI utility to fetch mock weather details by city name.

## Setup Instructions
Install requirements:
```bash
pip install -r requirements.txt
```

Run the main script:
```bash
python main.py London
```

## Running Tests
Run tests using pytest:
```bash
pytest tests/
```
