{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProjectMetadata",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Unique, clean name of the mini project."
    },
    "description": {
      "type": "string",
      "description": "A clear description of what the project does."
    },
    "author": {
      "type": "string",
      "description": "Name of the project author."
    },
    "difficulty": {
      "type": "string",
      "enum": ["Beginner", "Intermediate", "Advanced"],
      "description": "Difficulty level of the project."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Category tags associated with the project."
    },
    "dependencies": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of external packages required by the project."
    },
    "entry_point": {
      "type": "string",
      "description": "Main script file to execute (e.g. main.py)."
    }
  },
  "required": ["name", "description", "difficulty", "tags", "entry_point"]
}
